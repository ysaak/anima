{% extends "admin/layout" %}

{% block header %}
<div class="header-section">
    <h1>Utilisateurs</h1>
</div>
{% endblock %}

{% block content %}
<p class="text-right">
    <a href="{{ urlFor('admin.users.new') }}" class="btn btn-icon btn-primary"><span class="material-icons">add</span> Nouveau</a>
</p>

<div class="card card-body">
    <table class="table">
        <col>
        <col span="2" width="80">
        <thead class="thead-dark">
            <tr>
                <th>Nom</th>
                <th colspan="2">&nbsp;</th>
            </tr>
        </thead>
        <tbody>
    {% for user in userList %}
            <tr>
                <td>{{ user.name }}</td>
                <td><a href="{{ urlFor('admin.users.edit', { 'id': user.id }) }}">Modifier</a></td>
                <td><a class="text-danger" href="{{ urlFor('admin.users.delete', { 'id': user.id }) }}" data-toggle="confirm">Supprimer</a></td>
            </tr>
    {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}