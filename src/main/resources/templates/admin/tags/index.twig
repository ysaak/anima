{% extends "admin/layout" %}

{% block header %}
<div class="header-section">
    <h1>Tags</h1>
</div>
{% endblock %}

{% block content %}
<p class="text-right">
    <a href="{{ urlFor('admin.tags.new') }}" class="btn btn-icon btn-primary"><span class="material-icons">add</span> Nouveau</a>
</p>

<div class="card card-body">
    <table class="table">
        <col width="200">
        <col>
        <col span="2" width="80">
        <thead class="thead-dark">
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th colspan="2">&nbsp;</th>
            </tr>
        </thead>
        <tbody>
    {% for tag in tagList %}
            <tr>
                <td>{{ tag.name }}</td>
                <td>{{ tag.description }}</td>
                <td><a href="{{ urlFor('admin.tags.edit', { 'id': tag.id }) }}">Modifier</a></td>
                <td><a class="text-danger" href="{{ urlFor('admin.tags.delete', { 'id': tag.id }) }}" data-toggle="confirm">Supprimer</a></td>
            </tr>
    {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}